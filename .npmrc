# @vercel/analytics v2 は多数のフレームワークを optional peer に列挙しており、
# npm の peer 解決が未使用の @sveltejs/kit(→vite 8) を引き込み、vitest の
# vite 7 と ERESOLVE 衝突する。SvelteKit は実際には未使用のため、peer 解決を
# 緩めて回避する。ローカルと Vercel ビルド(npm install)の挙動を揃える目的。
legacy-peer-deps=true
